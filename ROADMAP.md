# ROADMAP

## site

### backlog

```
[ ] compare two or more items side by side
[ ] grey out and disable low scoring items instead of only striking them through
[ ] "X" button for sending a requirement to non-requirements without dragging
[ ] up/down arrows for reordering requirements without dragging
[ ] share a ranking as a link, so a reader can send someone their answer
[ ] remember a reader's ranking between visits
```

### complete

```
[x] static site output, publishable on github pages
[x] topics from topics.toml and from topics/<slug>.toml
[x] anti-requirements
[x] forced requirements
[x] relative urls, so the site works from any path
[x] validate topics at build time and name the file behind any error
[x] end to end tests that drive the real site in a browser
[x] publish each topic's data as json alongside its page
[x] `decide fmt` to sort topic files and fill in unevaluated requirements
[x] one time import of the hand-made topics from the legacy django database
[x] masonry layout, so a short section does not leave a gap beside a tall one
[x] dark mode, following the system theme
[x] the cards hold still while a requirement is being dragged between them
[x] `decide fmt` renumbers priorities 5 apart, keeping file order for ties
```

## data

### backlog

```
[ ] import a topic from a spreadsheet/csv
[ ] import a comparison table from wikipedia
```
