# every source is the project's own specification, manual or FAQ, named down
# to the section it was read from. these projects describe routing, keys and
# bootstrapping in different vocabularies, so a row that cannot be traced to
# the project's own words is left uncited rather than paraphrased.

requirements = [
	{ priority = -10, name = "Financial Instrument", force = true },
	{ priority = -5, name = "Bootstrap Nodes", force = false },
	{ priority = 5, name = "Perfect Forward Secrecy", force = false },
	{ priority = 10, name = "Private Routes", force = false },
	{ priority = 15, name = "Mobile Friendly", force = false },
	{ priority = 20, name = "Content Addressable Storage", force = false },
	{ priority = 25, name = "Bridged to Plainnet", force = false },
]

items = [
	{ name = "I2P", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "", sources = ["https://geti2p.net/en/docs/how/tech-intro", "the technical introduction specifies tunnels, garlic routing and the netDb, and no token or ledger"] },
		{ name = "Bootstrap Nodes", score = 1.0, comment = "HTTPS reseed servers", sources = ["https://geti2p.net/en/docs/how/network-database", "\"Bootstrapping\": \"volunteers publish their netDb directories (or a subset) on the regular (non-i2p) network, and the URLs of these directories are hardcoded in I2P\""] },
		{ name = "Perfect Forward Secrecy", score = 1.0, comment = "ECIES-X25519 ratchet", sources = ["https://geti2p.net/en/docs/how/tech-intro", "\"ECIES-X25519-AEAD-Ratchet\": \"X25519 is used for key exchange. ChaCha20/Poly1305 is used for authenticated symmetric encryption. Encryption keys are 'double ratcheted' or rotated periodically.\""] },
		{ name = "Private Routes", score = 1.0, comment = "both ends anonymous by default", sources = ["https://geti2p.net/en/docs/how/tech-intro", "\"I2P itself is not an outproxy network\": both ends of a connection are Destinations inside the network"] },
		{ name = "Mobile Friendly", score = 0.3, comment = "android build exists, heavy on battery", sources = ["https://geti2p.net/en/download/android", "the download page offers an Android build; the battery cost is not the page's claim"] },
		{ name = "Content Addressable Storage", score = 0.0, comment = "", sources = ["https://geti2p.net/en/docs/how/tech-intro", "\"I2P's focus should stay strictly on providing a generic anonymous communication layer, rather than as a component of Freenet\""] },
		{ name = "Bridged to Plainnet", score = 0.0, comment = "", sources = ["https://geti2p.net/en/docs/how/tech-intro", "\"no one is an outproxy by default, and running an outproxy doesn't automatically tell other people to proxy through you\""] },
	]},
	{ name = "Reticulum", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "", sources = ["https://reticulum.network/manual/understanding.html", "\"Reticulum is a networking stack suited for high-latency, low-bandwidth links\", with no token or ledger in the design goals"] },
		{ name = "Bootstrap Nodes", score = 0.3, comment = "explicit TCP endpoints", sources = ["https://reticulum.network/manual/interfaces.html", "\"type = TCPClientInterface\", each configured with a target host and port by hand"] },
		{ name = "Perfect Forward Secrecy", score = 1.0, comment = "ephemeral X25519 per link", sources = ["https://reticulum.network/manual/understanding.html", "\"Links additionally offer Forward Secrecy by default, employing an Elliptic Curve Diffie Hellman key exchange on Curve25519 to derive per-link ephemeral keys.\""] },
		{ name = "Private Routes", score = 0.0, comment = "", sources = ["https://reticulum.network/manual/understanding.html", "\"Both data sent over Links and single packets offer Initiator Anonymity\": the initiator only, not both ends"] },
		{ name = "Mobile Friendly", score = 0.7, comment = "very low bandwidth", sources = ["https://reticulum.network/manual/understanding.html", "\"Very low bandwidth requirements\": \"Reticulum should be able to function reliably over links with a transmission capacity as low as 5 bits per second.\""] },
		{ name = "Content Addressable Storage", score = 0.0, comment = "", sources = ["https://reticulum.network/manual/understanding.html", "\"Reticulum is at its core a message oriented system\": it moves packets, it does not store addressed content"] },
		{ name = "Bridged to Plainnet", score = 1.0, comment = "", sources = ["https://reticulum.network/manual/interfaces.html", "\"type = TCPClientInterface\" carries Reticulum over the ordinary Internet alongside its radio interfaces"] },
	]},
	{ name = "The Onion Router", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "", sources = ["https://community.torproject.org/onion-services/overview/", "the Tor Project's stated mission is \"to advance human rights and freedoms by creating and deploying free and open source anonymity and privacy technologies\", with no token or ledger"] },
		{ name = "Bootstrap Nodes", score = 1.0, comment = "", sources = ["https://community.torproject.org/relay/types-of-relays/", "\"The design of the Tor network means that the IP addresses of Tor relays (guard, middle, and exit) are public.\""] },
		{ name = "Perfect Forward Secrecy", score = 1.0, comment = "ntor handshake per circuit", sources = ["https://spec.torproject.org/tor-spec/create-created-cells.html", "\"The 'ntor' handshake\": \"0x0002 ntor - the ntor+curve25519+sha256 handshake\""] },
		{ name = "Private Routes", score = 1.0, comment = "", sources = ["https://community.torproject.org/onion-services/overview/", "\"Location hiding\": \"These circuits are anonymized circuits, so the server does not reveal the service location to the introduction points.\""] },
		{ name = "Mobile Friendly", score = 0.7, comment = "orbot and tor browser for android", sources = ["https://support.torproject.org/tormobile/", "\"Tor Mobile\""] },
		{ name = "Content Addressable Storage", score = 0.0, comment = "", sources = ["https://community.torproject.org/relay/types-of-relays/", "the relay types are guard, middle, exit and bridge: a circuit carries traffic, and no relay stores addressed content"] },
		{ name = "Bridged to Plainnet", score = 1.0, comment = "", sources = ["https://community.torproject.org/relay/types-of-relays/", "\"Exit relay\": \"The exit relay is the final relay in a Tor circuit, the one that sends traffic out to its destination.\""] },
	]},
	{ name = "Veilid", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "", sources = ["https://veilid.gitlab.io/developer-book/why-veilid/index.html", "\"We exist to develop, distribute, and maintain a privacy focused communication platform and protocol for the purposes of defending human and civil rights.\", with no token or ledger"] },
		{ name = "Bootstrap Nodes", score = 1.0, comment = "", sources = ["https://veilid.gitlab.io/developer-book/why-veilid/index.html", "\"Barring the initial bootstrap, the framework is completely Peer-to-Peer.\""] },
		{ name = "Perfect Forward Secrecy", score = 1.0, comment = "", sources = ["https://veilid.gitlab.io/developer-book/concepts/index.html", "the \"Cryptography\" section, and \"It is fully encrypted with built-in IP obfuscation\"; the book states the scheme without naming the ratchet"] },
		{ name = "Private Routes", score = 1.0, comment = "private by default", sources = ["https://veilid.gitlab.io/developer-book/why-veilid/index.html", "\"you don't have to share your actual IP or even node identity - Veilid has mechanisms in place to keep this data secure behind a private route\""] },
		{ name = "Mobile Friendly", score = 1.0, comment = "explicit design goal", sources = ["https://veilid.gitlab.io/developer-book/why-veilid/index.html", "\"Veilid is mobile-first ... taken into consideration from the very beginning, not reactively as an afterthought.\""] },
		{ name = "Content Addressable Storage", score = 0.3, comment = "block store designed, still a work in progress", sources = ["https://veilid.gitlab.io/developer-book/concepts/index.html", "\"Block Store\": \"File-like content will be stored in a content-addressable block store, a work in progress.\""] },
		{ name = "Bridged to Plainnet", score = 0.0, comment = "", sources = ["https://veilid.gitlab.io/developer-book/why-veilid/index.html", "\"Barring the initial bootstrap, the framework is completely Peer-to-Peer.\": there is no exit to the plain Internet"] },
	]},
	{ name = "yggdrasil", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "", sources = ["https://yggdrasil-network.github.io/faq.html", "the FAQ covers peering, routing and encryption, with no token or ledger"] },
		{ name = "Bootstrap Nodes", score = 0.3, comment = "explicit peer list, plus local multicast discovery", sources = ["https://yggdrasil-network.github.io/faq.html", "\"Yggdrasil requires that you configure either a static peer to another Yggdrasil node, or that you discover another Yggdrasil node on the same subnet using multicast discovery (which is enabled by default).\""] },
		{ name = "Perfect Forward Secrecy", score = 0.9, comment = "ratcheting session keys since v0.4", sources = ["https://yggdrasil-network.github.io/faq.html", "\"All traffic sent across the Yggdrasil Network is encrypted end-to-end, such that it cannot be decrypted or read by any intermediate nodes\"; the ratchet itself is in the release notes rather than the FAQ"] },
		{ name = "Private Routes", score = 0.0, comment = "", sources = ["https://yggdrasil-network.github.io/faq.html", "\"No, it is not a goal of the Yggdrasil project to provide anonymity. Direct peers over the Internet will be able to see your IP address\""] },
		{ name = "Mobile Friendly", score = 0.5, comment = "android app, but an always-on tunnel", sources = ["https://yggdrasil-network.github.io/installation.html", "\"Android: Yggdrasil is supported on Android with the following applications: Yggdrasil for Android\""] },
		{ name = "Content Addressable Storage", score = 0.0, comment = "", sources = ["https://yggdrasil-network.github.io/faq.html", "\"Yggdrasil does transport only IPv6 traffic internally\": it routes packets and stores nothing"] },
		{ name = "Bridged to Plainnet", score = 0.3, comment = "needs a NAT gateway node, not built in", sources = ["https://yggdrasil-network.github.io/faq.html", "\"Yggdrasil does transport only IPv6 traffic internally\", so reaching the plain Internet takes a gateway node"] },
	]},
]
