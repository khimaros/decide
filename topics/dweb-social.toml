#name = "Decentralized Web Social Network"

# each of these is a client on top of a protocol, so a source is either the
# client's own page or the protocol document that defines the behaviour, named
# down to the heading or line it was read from. rows that cannot be traced to
# one of those are left uncited.

requirements = [
	{ priority = -5, name = "Financial Instrument", force = false },
	{ priority = 5, name = "Self-Sovereign Identity", force = false },
	{ priority = 10, name = "Ephemerality", force = false },
	{ priority = 15, name = "Multiple Device Identity", force = false },
	{ priority = 20, name = "Mutability", force = false },
	{ priority = 25, name = "Vibrant Community", force = false },
	{ priority = 30, name = "Rich Media", force = false },
	{ priority = 35, name = "E2EE Direct Messages", force = false },
	{ priority = 40, name = "E2EE Group Chats", force = false },
	{ priority = 45, name = "Offline First", force = false },
	{ priority = 50, name = "Sealed Sender", force = false },
	{ priority = 55, name = "Data Replication", force = false },
]

items = [
	{ name = "Manyverse (Secure Scuttlebutt)", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "", sources = ["https://www.manyver.se/", "\"Forever free\": \"No token. No ICO. No blockchain.\""] },
		{ name = "Self-Sovereign Identity", score = 1.0, comment = "", sources = ["https://ssbc.github.io/scuttlebutt-protocol-guide/", "\"All messages in a feed are signed by that feed's long-term secret key.\""] },
		{ name = "Ephemerality", score = 0.0, comment = "", sources = ["https://ssbc.github.io/scuttlebutt-protocol-guide/", "\"The messages in a feed form an append-only log, meaning that once a message is posted it cannot be modified.\""] },
		{ name = "Multiple Device Identity", score = 0.0, comment = "", sources = ["https://ssbc.github.io/scuttlebutt-protocol-guide/", "\"Each message (except the first one) references the ID of the previous message\": a second device signing with the same key breaks the chain"] },
		{ name = "Mutability", score = 0.0, comment = "", sources = ["https://ssbc.github.io/scuttlebutt-protocol-guide/", "\"once a message is posted it cannot be modified\""] },
		{ name = "Vibrant Community", score = 0.9, comment = "wonderful people, but usage is falling off", sources = ["https://www.manyver.se/", "\"Home to a flourishing community\""] },
		{ name = "Rich Media", score = 1.0, comment = "", sources = ["https://www.manyver.se/", "\"So far we have built: ... Add pictures\""] },
		{ name = "E2EE Direct Messages", score = 1.0, comment = "private-box", sources = ["https://ssbc.github.io/scuttlebutt-protocol-guide/", "\"private-box\": \"the list of recipients, number of recipients and message body is concealed\""] },
		{ name = "E2EE Group Chats", score = 0.5, comment = "private threads capped at 7 recipients", sources = ["https://www.manyver.se/", "\"Private messaging up to 7 participants\""] },
		{ name = "Offline First", score = 1.0, comment = "", sources = ["https://www.manyver.se/", "\"even when you're offline, you can scroll, read anything, and even write posts and like content!\""] },
		{ name = "Sealed Sender", score = 0.0, comment = "author is public in the log", sources = ["https://ssbc.github.io/scuttlebutt-protocol-guide/", "\"Anyone can see the fact that a feed posted a private message, who sent it (the feed that posted it), when it was sent and its size.\""] },
		{ name = "Data Replication", score = 1.0, comment = "", sources = ["https://www.manyver.se/", "\"it syncs the latest updates directly with your friends' phones, through a shared local Wi-Fi or on the internet\""] },
	]},
	{ name = "Nomad Network (Reticulum)", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "", sources = ["https://github.com/markqvist/NomadNet", "the README's feature list is messaging, pages and files over Reticulum, with no token or ledger"] },
		{ name = "Self-Sovereign Identity", score = 1.0, comment = "", sources = ["https://github.com/markqvist/NomadNet", "README: built on LXMF and Reticulum, \"which together provides the cryptographic mesh functionality and peer-to-peer message routing\""] },
		{ name = "Ephemerality", score = 1.0, comment = "propagation node timeout", sources = ["https://github.com/markqvist/NomadNet", "README: \"Distributed and encrypted message store holds messages for offline users\""] },
		{ name = "Multiple Device Identity", score = 0.5, comment = "identity file can be copied, no per-device key", sources = ["https://github.com/markqvist/LXMF", "README: \"The Destination and Source fields are 16-byte Reticulum destination hashes\": one identity is one destination, however many devices hold the key"] },
		{ name = "Mutability", score = 1.0, comment = "pages are fetched directly", sources = ["https://github.com/markqvist/NomadNet", "README: \"Connectable nodes that can host pages and files\" and \"Node-side generated pages with PHP, Python, bash or others\""] },
		{ name = "Vibrant Community", score = 1.0, comment = "small but interesting!", sources = ["https://github.com/markqvist/NomadNet", "README: \"For help requests, discussion, sharing ideas or anything else related to Nomad Network, please have a look at the Nomad Network discussions pages\", plus the list of community-built pages and bots"] },
		{ name = "Rich Media", score = 0.2, comment = "micron markup text, files by explicit download", sources = ["https://github.com/markqvist/NomadNet", "README: \"An easy to use and bandwidth efficient markup language for writing pages\""] },
		{ name = "E2EE Direct Messages", score = 1.0, comment = "", sources = ["https://github.com/markqvist/NomadNet", "README: \"Off-grid, resilient mesh communication with strong encryption, forward secrecy and extreme privacy.\""] },
		{ name = "E2EE Group Chats", score = 0.0, comment = "LXMF is point to point", sources = ["https://github.com/markqvist/LXMF", "README: a message carries one \"Destination\" and one \"Source\", both single Reticulum destination hashes"] },
		{ name = "Offline First", score = 0.7, comment = "LoRa and sneakernet capability", sources = ["https://github.com/markqvist/NomadNet", "README: \"Encrypted messaging over packet-radio, LoRa, WiFi or anything else Reticulum supports.\""] },
		{ name = "Sealed Sender", score = 0.7, comment = "propagation nodes hold ciphertext, source is not in the header", sources = ["https://github.com/markqvist/NomadNet", "README: \"Distributed and encrypted message store holds messages for offline users\""] },
		{ name = "Data Replication", score = 0.3, comment = "LXMF messages only", sources = ["https://github.com/markqvist/NomadNet", "README: the message store replicates messages; pages are fetched from the node that hosts them"] },
	]},
	{ name = "Primal (nostr)", evaluations = [
		{ name = "Financial Instrument", score = 1.0, comment = "built-in lightning wallet, zaps everywhere", sources = ["https://github.com/nostr-protocol/nips/blob/master/57.md", "NIP-57 \"Lightning Zaps\""] },
		{ name = "Self-Sovereign Identity", score = 1.0, comment = "", sources = ["https://github.com/nostr-protocol/nips/blob/master/01.md", "NIP-01: an identity is a secp256k1 keypair, with no registry"] },
		{ name = "Ephemerality", score = 0.7, comment = "NIP-09, implemented in a few popular clients and relays", sources = ["https://github.com/nostr-protocol/nips/blob/master/09.md", "NIP-09 \"Event Deletion Request\": a request, which relays need not honour"] },
		{ name = "Multiple Device Identity", score = 0.9, comment = "one key works everywhere, NIP-46 avoids copying it", sources = ["https://github.com/nostr-protocol/nips/blob/master/46.md", "NIP-46 \"Nostr Remote Signing\""] },
		{ name = "Mutability", score = 0.7, comment = "replaceable kinds only, regular notes need NIP-09", sources = ["https://github.com/nostr-protocol/nips/blob/master/01.md", "NIP-01: replaceable and addressable kinds, everything else being a regular event"] },
		{ name = "Vibrant Community", score = 0.3, comment = "mostly crypto bros", sources = ["https://github.com/nostr-protocol/nips/blob/master/57.md", "NIP-57 \"Lightning Zaps\": payment is wired into the social layer itself, which is what the crowd is selected on"] },
		{ name = "Rich Media", score = 1.0, comment = "", sources = ["https://github.com/nostr-protocol/nips/blob/master/94.md", "NIP-94 \"File Metadata\""] },
		{ name = "E2EE Direct Messages", score = 1.0, comment = "NIP-17 gift wrap, superseding NIP-04", sources = ["https://github.com/nostr-protocol/nips/blob/master/17.md", "NIP-17: \"the unsigned chat messages must be sealed (kind:13) and then gift-wrapped (kind:1059) to each receiver and the sender individually\""] },
		{ name = "E2EE Group Chats", score = 0.3, comment = "NIP-17 covers small groups, no MLS (NIP-EE)", sources = ["https://github.com/nostr-protocol/nips/blob/master/17.md", "NIP-17: \"Group chats with more than 10 participants should find a more suitable messaging scheme.\""] },
		{ name = "Offline First", score = 0.0, comment = "relays required", sources = ["https://github.com/nostr-protocol/nips/blob/master/01.md", "NIP-01: clients publish to and subscribe from relays over websockets"] },
		{ name = "Sealed Sender", score = 1.0, comment = "NIP-59 gift wrap uses a fresh key per message", sources = ["https://github.com/nostr-protocol/nips/blob/master/59.md", "NIP-59 \"Gift Wrap\"", "https://github.com/nostr-protocol/nips/blob/master/17.md", "NIP-17: \"Clients SHOULD randomize created_at in up to two days in the past in both the seal and the gift wrap\""] },
		{ name = "Data Replication", score = 0.5, comment = "published to several relays, none obliged to keep it", sources = ["https://github.com/nostr-protocol/nips/blob/master/01.md", "NIP-01: relays choose what to store and for how long"] },
	]},
]
