#name = "Decentralized Web Protocol"

requirements = [
	{ priority = -10, name = "Financial Instrument", force = false },
	{ priority = 20, name = "Ephemerality", force = false },
	{ priority = 20, name = "Multiple Device Identity", force = false },
	{ priority = 20, name = "Mutability", force = false },
	{ priority = 40, name = "Offline First", force = false },
]

items = [
	{ name = "Secure Scuttlebutt", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "" },
		{ name = "Ephemerality", score = 0.0, comment = "append-only log, nothing expires" },
		{ name = "Multiple Device Identity", score = 0.0, comment = "one feed per key, forks break replication" },
		{ name = "Mutability", score = 0.0, comment = "" },
		{ name = "Offline First", score = 1.0, comment = "" },
	]},
	{ name = "Willow", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "" },
		{ name = "Ephemerality", score = 0.4, comment = "prefix pruning and payload forgetting, but no time-based expiry" },
		{ name = "Multiple Device Identity", score = 1.0, comment = "meadowcap delegates subspace writes to per-device keys" },
		{ name = "Mutability", score = 1.0, comment = "newer entry at a path replaces the old one" },
		{ name = "Offline First", score = 1.0, comment = "state-based CRDT, sneakernet drops" },
	]},
	{ name = "earthstar", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "" },
		{ name = "Ephemerality", score = 1.0, comment = "deleteAfter timestamps, expiry enforced network-wide" },
		{ name = "Multiple Device Identity", score = 0.7, comment = "same keypair copied per device, no per-device key" },
		{ name = "Mutability", score = 1.0, comment = "latest timestamp per path wins" },
		{ name = "Offline First", score = 1.0, comment = "" },
	]},
	{ name = "nostr", evaluations = [
		{ name = "Financial Instrument", score = 0.8, comment = "no native token, but lightning zaps (NIP-57) are pervasive" },
		{ name = "Ephemerality", score = 0.8, comment = "ephemeral kinds 20000-29999; NIP-09 deletion is advisory" },
		{ name = "Multiple Device Identity", score = 0.9, comment = "one key works everywhere; NIP-46 avoids copying it" },
		{ name = "Mutability", score = 0.7, comment = "replaceable kinds only; regular notes need NIP-09" },
		{ name = "Offline First", score = 0.0, comment = "relays required" },
	]},
	{ name = "p2panda", evaluations = [
		{ name = "Financial Instrument", score = 0.0, comment = "" },
		{ name = "Ephemerality", score = 0.5, comment = "expiry and pruning specified, not yet implemented" },
		{ name = "Multiple Device Identity", score = 1.0, comment = "key pair reuse across devices, forks tolerated" },
		{ name = "Mutability", score = 1.0, comment = "documents take UPDATE and DELETE operations" },
		{ name = "Offline First", score = 1.0, comment = "delay tolerant, out-of-order arrival expected" },
	]},
]
